{% extends 'job/admin_base2.html' %}
{% from "macros.html" import pagination_form %}
{% block users %}
<div class="container">
<div class="row">
<div class="col-xs-12 col-md-12"> 
<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading">职业信息列表</div>
  </div>
<table class="table">
  <thead>

  <tr>
    <th>ID</th>
    <th>职位名称</th>
    <th>企业</th>
    <th>投递时间</th>
  </tr>
  </thead>
  <tbody>
{% for jobs in message.Jobm %}
   {% if jobs.job_state2 != "待删除" %}
  <tr>
    <td>{{ jobs.id }}</td>
    <td>{{ jobs.jobname }}</td>
    <td>{{ jobs.Jobid.eusername }}</td>
    <td>{{ message.create_time(jobs.id) }}</td>
    <td>{{ message.statess(jobs_id) }}</td>
  </tr>

   {% endif %}
{% endfor %}
  </tbody>
</table

</div>
</div>
</div>
   <div class="row">
   </div>
  <!-- Default panel contents -->
{% endblock %}
